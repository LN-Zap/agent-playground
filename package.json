{
  "name": "agent-playground",
  "description": "A development environment for the Agent Playground project.",
  "author": "Tom Kirkpatrick <tom@kirk patrick.me>",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "simple-git-hooks",
    "postinstall": "npx rulesync install && npx rulesync generate --delete",
    "rulesync": "npx rulesync",
    "docs:build": "mkdocs build",
    "docs:serve": "mkdocs serve",
    "docs:deploy": "mkdocs gh-deploy"
  },
  "simple-git-hooks": {
    "post-merge": "npx rulesync install && npx rulesync generate --delete",
    "post-checkout": "npx rulesync install && npx rulesync generate --delete"
  },
  "dependencies": {
    "rulesync": "7.2.0"
  },
  "devDependencies": {
    "simple-git-hooks": "2.13.1"
  }
}
