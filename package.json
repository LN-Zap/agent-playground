{
  "name": "agent-playground",
  "description": "A development environment for the Agent Playground project.",
  "author": "Tom Kirkpatrick <tom@kirk patrick.me>",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "simple-git-hooks",
    "postinstall": "npx rulesync install && npx rulesync generate",
    "rulesync": "npx rulesync"
  },
  "simple-git-hooks": {
    "post-merge": "npx rulesync install && npx rulesync generate",
    "post-checkout": "npx rulesync install && npx rulesync generate"
  },
  "devDependencies": {
    "simple-git-hooks": "^2.13.1"
  },
  "dependencies": {
    "rulesync": "7.2.0"
  }
}
