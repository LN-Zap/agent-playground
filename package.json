{
  "name": "agent-playground",
  "description": "A set of patterns for portable agent configuration and tooling.",
  "author": "Tom Kirkpatrick <tom@strike.me>",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "simple-git-hooks",
    "postinstall": "npm run --silent rulesync:sync",
    "rulesync:sync": "npx rulesync install --frozen && npx rulesync generate --delete",
    "rulesync:update": "npx rulesync install --update && npx rulesync generate --delete",
    "rulesync:verify": "npm run --silent rulesync:sync && npx rulesync generate --check"
  },
  "simple-git-hooks": {
    "post-merge": "npm run --silent rulesync:sync",
    "post-checkout": "npm run --silent rulesync:sync"
  },
  "devDependencies": {
    "rulesync": "7.10.0",
    "simple-git-hooks": "2.13.1"
  }
}
